steps:
  # Build the Docker image
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-t'
      - 'gcr.io/modular-analog-476221-h8/speakeasy-backend:latest'
      - '.'

  # Push the image to Google Container Registry
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - 'gcr.io/modular-analog-476221-h8/speakeasy-backend:latest'

  # Deploy to Cloud Run
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      - 'run'
      - 'deploy'
      - 'speakeasy-backend'
      - '--image'
      - 'gcr.io/modular-analog-476221-h8/speakeasy-backend:latest'
      - '--region'
      - 'us-central1'
      - '--platform'
      - 'managed'
      - '--allow-unauthenticated'
      # Public configuration (not secrets)
      - '--set-env-vars'
      - 'GOOGLE_CLIENT_ID=823510409781-s5d3hrffelmjcl8kjvchcv3tlbp0shbo.apps.googleusercontent.com,APPLE_TEAM_ID=E7B9UE64SF,APPLE_CLIENT_ID=com.speakeasy.webapp,APPLE_KEY_ID=864SJW3HGZ,GCP_PROJECT_ID=modular-analog-476221-h8'
      # Mount secrets from Secret Manager (run scripts/setup-secrets.sh first)
      - '--update-secrets'
      - 'OPENAI_API_KEY=openai-api-key:latest,APPLE_PRIVATE_KEY_BASE64=apple-private-key:latest,STRIPE_SECRET_KEY=stripe-secret-key:latest,JWT_SECRET=jwt-secret:latest'
      - '--memory'
      - '2Gi'
      - '--cpu'
      - '2'
      - '--min-instances'
      - '0'
      - '--max-instances'
      - '10'
      - '--timeout'
      - '60s'

# Images to store in Container Registry
images:
  - 'gcr.io/modular-analog-476221-h8/speakeasy-backend:latest'

# Options
options:
  logging: CLOUD_LOGGING_ONLY
  machineType: 'N1_HIGHCPU_8'


